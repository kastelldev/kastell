---
phase: 29-backup-schedule
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/core/backupSchedule.ts
  - src/core/__tests__/backupSchedule.test.ts
autonomous: true
requirements:
  - BKUP-01
  - BKUP-02
  - BKUP-03
  - BKUP-04
  - BKUP-05

must_haves:
  truths:
    - "After scheduleBackup() returns success, VPS crontab has exactly one kastell-backup line"
    - "After scheduleBackup() returns success, /root/kastell-backup.sh exists on VPS with flock overlap protection"
    - "listBackupSchedule() returns the cron expression currently active on the VPS"
    - "After removeBackupSchedule() returns success, VPS crontab has zero kastell-backup lines"
    - "Running scheduleBackup() twice with different cron expressions replaces (not duplicates) the entry"
    - "Local schedules.json reflects the last schedule operation per server"
  artifacts:
    - path: "src/core/backupSchedule.ts"
      provides: "scheduleBackup, listBackupSchedule, removeBackupSchedule, command builders, local schedule persistence"
      exports: ["scheduleBackup", "listBackupSchedule", "removeBackupSchedule", "buildDeployBackupScriptCommand", "buildInstallCronCommand", "buildListCronCommand", "buildRemoveCronCommand", "getSchedules", "saveSchedule", "removeSchedule", "ScheduleResult", "ListScheduleResult"]
    - path: "src/core/__tests__/backupSchedule.test.ts"
      provides: "Unit tests for all core backupSchedule functions"
      min_lines: 80
  key_links:
    - from: "src/core/backupSchedule.ts"
      to: "src/utils/ssh.ts"
      via: "sshExec for remote crontab manipulation"
      pattern: "sshExec\\("
    - from: "src/core/backupSchedule.ts"
      to: "src/utils/config.ts"
      via: "CONFIG_DIR for schedules.json path"
      pattern: "CONFIG_DIR"
---

<objective>
Create the core backup schedule module with command builders for cron management, local schedule persistence, and the three orchestrator functions (schedule, list, remove).

Purpose: Establishes all business logic for remote cron-based backup scheduling, with flock overlap protection and marker-comment idempotency. Plan 02 will wire this to the CLI.
Output: `src/core/backupSchedule.ts` with full test coverage.
</objective>

<execution_context>
@C:/Users/Omrfc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Omrfc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-backup-schedule/29-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/utils/ssh.ts:
```typescript
export function sshExec(ip: string, command: string, options?: { timeout?: number }): Promise<{ code: number; stdout: string; stderr: string }>;
export function assertValidIp(ip: string): void; // sync throw if invalid
export function checkSshAvailable(): boolean;
```

From src/utils/config.ts:
```typescript
export const CONFIG_DIR: string; // join(homedir(), ".kastell")
export const BACKUPS_DIR: string;
```

From src/core/lock.ts (pattern reference):
```typescript
export interface LockResult {
  success: boolean;
  steps: LockStepResult;
  scoreBefore?: number;
  scoreAfter?: number;
  error?: string;
  hint?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core backupSchedule module with TDD</name>
  <files>src/core/backupSchedule.ts, src/core/__tests__/backupSchedule.test.ts</files>
  <behavior>
    - buildDeployBackupScriptCommand() returns string containing: shebang, flock -n 200, kastell-backup marker, docker/coolify runtime detection, /var/backups/kastell/ output path, chmod +x
    - buildInstallCronCommand("0 3 * * *") returns string containing grep -v '# kastell-backup' (remove old) and echo with '# kastell-backup' marker (append new)
    - buildListCronCommand() returns string with grep '# kastell-backup'
    - buildRemoveCronCommand() returns string with grep -v '# kastell-backup' piped to crontab -
    - scheduleBackup(ip, serverName, cronExpr) calls sshExec twice (deploy script + install cron), saves to local schedules.json, returns { success: true }
    - scheduleBackup returns { success: false, error } when deploy script sshExec fails (code !== 0)
    - scheduleBackup returns { success: false, error } when cron install sshExec fails (code !== 0)
    - scheduleBackup calls assertValidIp before any SSH call
    - listBackupSchedule(ip, serverName) calls sshExec with list command, returns { success: true, cronExpr } parsed from stdout
    - listBackupSchedule returns localCronExpr from schedules.json when available
    - removeBackupSchedule(ip, serverName) calls sshExec with remove command, deletes from local schedules.json
    - getSchedules() returns empty object when file does not exist
    - saveSchedule(serverName, cronExpr) writes to schedules.json
    - removeSchedule(serverName) removes entry from schedules.json
    - Minimal cron expression validation: rejects empty string, accepts 5-field expressions
  </behavior>
  <action>
    RED phase: Create `src/core/__tests__/backupSchedule.test.ts` with tests for all behaviors listed above. Mock `sshExec`, `assertValidIp` from `../../utils/ssh`, and mock `fs` (readFileSync, writeFileSync, existsSync, mkdirSync) for schedules.json operations. Use `jest.mock` pattern from existing tests. Run tests — all must FAIL.

    GREEN phase: Create `src/core/backupSchedule.ts` implementing:

    1. **Types**: `ScheduleResult { success, error?, hint? }`, `ListScheduleResult { success, cronExpr?, localCronExpr?, error? }`

    2. **Local schedule persistence** (schedules.json):
       - `SCHEDULES_FILE = join(CONFIG_DIR, "schedules.json")`
       - `getSchedules(): Record<string, string>` — reads file, returns `{}` if missing/invalid
       - `saveSchedule(serverName, cronExpr)` — reads, merges, writes with mode 0o600
       - `removeSchedule(serverName)` — reads, deletes key, writes

    3. **Command builders** (pure functions, no SSH):
       - `buildDeployBackupScriptCommand()` — heredoc deploying `/root/kastell-backup.sh` with:
         - `#!/bin/bash`
         - `exec 200>/tmp/kastell-backup.lock` + `flock -n 200 || exit 0`
         - Runtime detection: `docker ps | grep coolify` -> Coolify pg_dump, else bare tar
         - Output to `/var/backups/kastell/` with date-stamped filename
         - Log to `/var/log/kastell-backup.log`
         - Ends with `KASTELL_EOF` + `chmod +x /root/kastell-backup.sh`
       - `buildInstallCronCommand(cronExpr)` — `(crontab -l 2>/dev/null | grep -v '# kastell-backup'; echo "${cronExpr} /root/kastell-backup.sh # kastell-backup") | crontab -`
       - `buildListCronCommand()` — `crontab -l 2>/dev/null | grep '# kastell-backup' || echo ""`
       - `buildRemoveCronCommand()` — `(crontab -l 2>/dev/null | grep -v '# kastell-backup') | crontab -`

    4. **Orchestrators** (async, use sshExec):
       - `scheduleBackup(ip, serverName, cronExpr)` — assertValidIp, deploy script, install cron, saveSchedule, return result
       - `listBackupSchedule(ip, serverName)` — assertValidIp, sshExec list command, parse cron from stdout, also read localCronExpr from schedules.json
       - `removeBackupSchedule(ip, serverName)` — assertValidIp, sshExec remove command, removeSchedule locally

    5. **Validation helper**: `validateCronExpr(expr)` — rejects empty, checks 5 whitespace-separated fields matching `[0-9*,/-]+` pattern. Returns `{ valid, error? }`.

    Use `import { sshExec, assertValidIp } from "../utils/ssh.js"` and `import { CONFIG_DIR } from "../utils/config.js"`.

    Run tests — all must PASS.

    REFACTOR if needed, then run full suite `npm test` to ensure no regressions.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="backupSchedule" --passWithNoTests --no-coverage</automated>
  </verify>
  <done>All backupSchedule tests pass. Command builders produce correct shell commands with marker pattern and flock. Orchestrators correctly sequence SSH calls. Local schedules.json persistence works. Full suite green.</done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern="backupSchedule"` passes all tests
- `npm test` full suite passes (no regressions)
- `npm run build` compiles without errors
</verification>

<success_criteria>
- src/core/backupSchedule.ts exports all 3 orchestrators + 4 command builders + 3 schedule persistence functions + types
- All command builders produce correct shell commands (marker comment, flock, heredoc)
- scheduleBackup correctly sequences: assertValidIp -> deploy script -> install cron -> save local
- Error paths tested (SSH failure on deploy, SSH failure on cron install)
- schedules.json read/write/delete works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/29-backup-schedule/29-01-SUMMARY.md`
</output>
