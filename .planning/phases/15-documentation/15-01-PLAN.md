---
phase: 15-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.tr.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "README.md documents Dokploy support with --mode dokploy examples for init, add, update, maintain, logs"
    - "README.md documents DX features: --dry-run, completions, config validate, version check"
    - "README.md reflects current test count (2,266 tests / 86 suites)"
    - "README.md describes TUI enhancements: ASCII logo, emoji categories, search menu"
    - "README.md What's Next lists v1.5+ items only (no shipped v1.4 features)"
    - "README.tr.md mirrors all README.md updates in Turkish"
    - "README.md header links to README.tr.md with proper Turkish: Türkçe (not Turkce)"
  artifacts:
    - path: "README.md"
      provides: "English documentation for v1.4"
      contains: "--mode dokploy"
    - path: "README.tr.md"
      provides: "Turkish documentation for v1.4"
      contains: "--mode dokploy"
  key_links:
    - from: "README.md"
      to: "README.tr.md"
      via: "language toggle link in header"
      pattern: "Türkçe"
---

<objective>
Update README.md and README.tr.md to accurately document all v1.4 features.

Purpose: Phase 15 closes out v1.4 by ensuring documentation matches the shipped feature set. Currently both READMEs are stuck at v1.3 state -- missing Dokploy support, DX features, TUI enhancements, and showing stale test counts.

Output: Two updated README files reflecting the complete v1.4 feature set.
</objective>

<execution_context>
@C:/Users/Omrfc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Omrfc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-documentation/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md (English) for v1.4</name>
  <files>README.md</files>
  <action>
Update README.md with all v1.4 changes. Use the gap analysis from 15-RESEARCH.md as the definitive checklist. Specific changes:

1. **Header language link**: Change `[Turkce](README.tr.md)` to `[Türkçe](README.tr.md)` (proper Turkish characters).

2. **Quick Start section**: Update the interactive menu example to reflect the new TUI:
   - Show the KASTELL ASCII logo hint (mention branded logo appears on launch)
   - Update menu example to show emoji category icons and search-style prompt (type to filter, not just arrow keys)
   - Update description text to mention search/filter capability

3. **Deploy section**: Add `kastell init --mode dokploy` example alongside existing `--mode bare` line.

4. **Manage section**: Add `kastell config validate` example.

5. **Update and Maintain section**: Replace Coolify-only description with platform-aware description:
   - `kastell update my-server` -- "Update platform (Coolify or Dokploy, auto-detected from server record)"
   - Add `kastell update my-server --dry-run` example showing dry-run capability
   - Add `kastell maintain my-server --dry-run` example

6. **Monitor and Debug section**: Update `kastell logs` description to mention it works for both Coolify and Dokploy (Docker Swarm service names).

7. **Add new "Supported Platforms" section** (after Supported Providers):
   - Coolify (default) -- Docker-based PaaS
   - Dokploy -- Docker Swarm-based PaaS
   - Bare -- Generic VPS (no platform)
   - Mention PlatformAdapter architecture: same commands work across platforms

8. **Add new "Developer Experience" section** (after Supported Platforms or after Templates):
   - `--dry-run` flag: Available on destructive commands (destroy, update, restart, remove, maintain, restore, firewall, domain, backup, snapshot, secure). Preview what would happen without executing.
   - Shell completions: `kastell completions bash|zsh|fish`
   - Config validation: `kastell config validate`
   - Version check: `kastell --version` shows update notification if newer version exists

9. **Security section**: Update test count from "2,191 tests across 84 suites" to "2,266 tests across 86 suites".

10. **MCP tools table**: Update descriptions that say "Coolify" to say "Coolify/Dokploy" where applicable:
    - server_info: "Query server information, check cloud provider and platform status"
    - server_logs: "Fetch platform/Docker logs and system metrics via SSH"
    - server_maintain: "Update platform, restart servers, run full maintenance"

11. **What's Next section**: Remove v1.4 items (shipped). Replace with:
    - `kastell audit` -- security scanning and compliance reports (v1.5)
    - kastell.dev website (v1.5)
    - Guard daemon and fleet management (v1.6)
    - Plugin ecosystem for AI IDEs (v2.0)

12. **Troubleshooting**: Update "Server not responding?" to not be Coolify-specific. Change to mention platform-aware status check.

Do NOT change: badges, license, support links, philosophy, contributing, installation, YAML config, templates, providers sections (unless adding Dokploy examples to YAML).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r=fs.readFileSync('README.md','utf8'); const checks=['2,266 tests','--mode dokploy','--dry-run','completions bash','config validate','Türkçe','kastell audit']; const missing=checks.filter(c=>!r.includes(c)); if(missing.length){console.error('Missing:',missing);process.exit(1)}else{console.log('All v1.4 content present in README.md')}"</automated>
  </verify>
  <done>README.md contains Dokploy examples, DX features, updated test counts, TUI description, and v1.5+ roadmap in What's Next. Header links to README.tr.md with "Türkçe".</done>
</task>

<task type="auto">
  <name>Task 2: Update README.tr.md (Turkish) to mirror v1.4 changes</name>
  <files>README.tr.md</files>
  <action>
Mirror ALL changes from Task 1 into README.tr.md with Turkish translations. Use the updated README.md as the structural reference -- both files must have identical section structure.

Specific changes (same 12 points as Task 1, in Turkish):

1. **Header**: Already correct ("Türkçe"). Verify and keep.

2. **Hizli Baslangic**: Update interactive menu description and example to reflect search prompt, ASCII logo, emoji categories. Turkish description of type-to-filter.

3. **Kurulum (Deploy)**: Add `kastell init --mode dokploy` example.

4. **Yonetim (Manage)**: Add `kastell config validate`.

5. **Guncelleme ve Bakim**: Replace Coolify-only with platform-aware:
   - "Platformu guncelle (Coolify veya Dokploy, sunucu kaydından otomatik algılanır)"
   - Add `--dry-run` examples

6. **Izleme ve Hata Ayiklama**: Update logs description for Coolify+Dokploy.

7. **Add "Desteklenen Platformlar" section** (Turkish equivalent of Supported Platforms):
   - Coolify (varsayılan) -- Docker tabanlı PaaS
   - Dokploy -- Docker Swarm tabanlı PaaS
   - Bare -- Genel VPS (platform yok)

8. **Add "Gelistirici Deneyimi" section** (Turkish equivalent of Developer Experience):
   - `--dry-run`, completions, config validate, version check with Turkish descriptions

9. **Guvenlik**: Update from "78 test suite'inde 2.099 test" to "86 test suite'inde 2.266 test".

10. **MCP table**: Update Coolify-specific descriptions to Coolify/Dokploy.

11. **Gelecek Planlar**: Remove shipped items ("Dokploy platform destegi", "--platform dokploy"). Replace with v1.5+ items:
    - `kastell audit` -- guvenlik taramasi ve uyumluluk raporlari (v1.5)
    - kastell.dev web sitesi (v1.5)
    - Guard daemon ve fleet yonetimi (v1.6)
    - AI IDE'ler icin plugin ekosistemi (v2.0)

12. **Sorun Giderme**: Update to be platform-neutral (not Coolify-specific).

IMPORTANT: Use proper Turkish characters throughout (ı, ş, ç, ğ, ö, ü, İ, Ş, Ç, Ğ, Ö, Ü). The existing file already uses them correctly -- maintain that standard.

Verify section count matches README.md when done.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r=fs.readFileSync('README.tr.md','utf8'); const checks=['2.266 test','--mode dokploy','--dry-run','completions bash','config validate','kastell audit','Desteklenen Platformlar']; const missing=checks.filter(c=>!r.includes(c)); if(missing.length){console.error('Missing:',missing);process.exit(1)}else{console.log('All v1.4 content present in README.tr.md')}"</automated>
  </verify>
  <done>README.tr.md mirrors all README.md v1.4 updates in Turkish. Test counts, Dokploy examples, DX features, TUI description, and v1.5+ roadmap all present. Section structure matches English version.</done>
</task>

</tasks>

<verification>
Both README files updated with identical section structure covering all v1.4 features.
Run verification scripts for both files to confirm key content strings are present.
</verification>

<success_criteria>
1. README.md header says "Türkçe" (not "Turkce")
2. Both files document Dokploy with `--mode dokploy` examples
3. Both files show 2,266 tests / 86 suites
4. Both files document --dry-run, completions, config validate, version check
5. Both files describe TUI search menu, ASCII logo, emoji categories
6. Both files have What's Next pointing to v1.5+ (no shipped v1.4 items)
7. MCP tool descriptions are platform-neutral (Coolify/Dokploy)
8. Section structure matches between EN and TR versions
</success_criteria>

<output>
After completion, create `.planning/phases/15-documentation/15-01-SUMMARY.md`
</output>
