/**
 * Report formatters (HTML + Markdown) for audit results.
 * Stub — full implementation in Task 2.
 */

import type { AuditResult } from "../types.js";

/** Format audit result as single-file HTML report */
export function formatHtmlReport(_result: AuditResult): string {
  return "<!-- stub -->";
}

/** Format audit result as Markdown report */
export function formatMdReport(_result: AuditResult): string {
  return "<!-- stub -->";
}
