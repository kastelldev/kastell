/**
 * SVG badge formatter for audit results.
 * Stub — full implementation in Task 2.
 */

import type { AuditResult } from "../types.js";

/** Format audit result as SVG badge */
export function formatBadge(_result: AuditResult): string {
  return "<!-- stub -->";
}
